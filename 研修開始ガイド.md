# R2B研修 — 完全セットアップガイド

研修生がR2B研修を開始するための、ステップバイステップの手引きです。

---

## 📋 全体の流れ

```
1. リポジトリをクローン
   ↓
2. メインブランチを作成（{あなたの名前}）
   ↓
3. (r2b ルートで) Marketplace 追加 → Plugin インストール → /r2b-init
   ↓
4. Sprint ループ（以下を Sprint 1→2→3→4 で繰り返し）
   ├─ メインブランチから Sprint{N} ブランチを作成
   ├─ training-sprint{N} に cd
   ├─ Marketplace 削除・再追加
   ├─ Learn（Webapp）→ /r2b-design → /r2b-build → /r2b-review
   ├─ git push で Sprint{N} ブランチに保存
   ├─ メインブランチに戻る（git switch {名前}）
   ├─ Sprint{N} をメインブランチにマージ（git merge）
   ├─ メインブランチを push
   └─ 次の Sprint{N+1} を作成（git switch -c）
   ↓
5. Sprint 1, 2, 3, 4 をすべて完了
   ↓
6. メインブランチに最終 push（全 Sprint の成果が統合）
```

---

## 🚀 ステップ1: リポジトリをクローン

### 1-1. GitHub から r2b リポジトリをクローン

```bash
git clone git@github.com:SALT2-Boost/r2b.git
cd r2b
```

**クローンされたディレクトリ構造：**

```
r2b/
├── db/                         # データベース
│   ├── schema-master.sql       # マスターDBスキーマ
│   ├── schema-personal.sql     # 個人DBスキーマ
│   ├── schema-quiz.sql         # クイズDBスキーマ
│   ├── master.sql              # マスターDB初期データ
│   ├── quiz.sql                # クイズDB初期データ
│   ├── master.sqlite           # マスターDB（初期化済み）
│   └── quiz.sqlite             # クイズDB（初期化済み）
├── webapp/                     # R2B Progress Viewer
│   ├── server.py
│   └── run.sh
├── training-sprint1/           # Sprint 1 リポジトリ
│   ├── src/                    # ソースコード
│   └── docs/                   # 設計・学習ドキュメント
│       ├── requirements/       # 設計書
│       └── review/             # レビュードキュメント
├── training-sprint2/           # Sprint 2 リポジトリ
├── training-sprint3/           # Sprint 3 リポジトリ
└── training-sprint4/           # Sprint 4 リポジトリ
```

---

## 👤 ステップ2: メインブランチを作成（初期化用）

### 2-1. メインブランチを作成

```bash
git switch -c {あなたの名前}
```

例：
```bash
git switch -c 田中太郎
```

### 2-2. ブランチが作成されたか確認

```bash
git branch
# 出力例：
#   main
# * 田中太郎
```

このブランチで `/r2b-init` を実行します。

---

## 🌳 ステップ2.5: Sprint 用ブランチについて

**ブランチ命名ルール:**

各 Sprint に進む前に、Sprint 専用のサブブランチを作成します：

```
main
  ↓
{あなたの名前}  (初期化用・/r2b-init はここで実行)
  ↓
{あなたの名前-sprint1}  ← Sprint 1 作業用
{あなたの名前-sprint2}  ← Sprint 2 作業用
{あなたの名前-sprint3}  ← Sprint 3 作業用
{あなたの名前-sprint4}  ← Sprint 4 作業用
```

**詳細は後述の「各 Sprint での学習フロー」を参照**

---
## 🔌 ステップ3: R2B プラグインをセットアップ（r2b ルートで一度だけ実行）

### 3-1. Claude Code で r2b ディレクトリを開く

```bash
# r2b リポジトリのルートディレクトリで Claude Code を起動
cd r2b
claude code .
```

### 3-2. Marketplace にリポジトリを追加

Claude Code のターミナルで以下を実行：

```bash
/plugin marketplace add https://github.com/SALT2-Boost/r2b-docs.git#main
```

**確認:**
```bash
/plugin marketplace list
# r2b が表示される
```

### 3-3. プラグインをインストール

```bash
/plugin install r2b
```

インストール方法を選択する画面が表示されたら、**「Only for you, only in this repo」** を選択してください。

### 3-4. 🔄 セッションを再起動（重要）

Claude Code 内でセッションを終了し、新しいセッションを開く：

```bash
exit
# ターミナルが閉じたら、新しい Claude Code セッションを開く
```

### 3-5. /r2b-init を実行（初期化は一度だけ）

```bash
/r2b-init
```

**実行内容:**
- 研修者の名前を聞かれるので入力
- `db/personal.sqlite` が作成される
- 環境チェック（Node.js, Python, VSCode, Git の確認）
- Claude Code の基本操作説明
- Sprint 1 の準備
- **Webapp が自動起動**（http://localhost:8000）

完了メッセージが表示されたら、セットアップは終了です！

---

## ✅ 各Sprint での学習フロー

以下の手順を **Sprint 1, 2, 3, 4 で繰り返します。**

### Step 0: Sprint 用ブランチを作成

メインブランチから Sprint 専用のブランチを作成します：

例: Sprint 1 の場合

```bash
# メインブランチ（{あなたの名前}）にいることを確認
git branch
# * 田中太郎

# Sprint 1 用のブランチを作成
git switch -c 田中太郎-sprint1
```

例: Sprint 2 の場合

```bash
# メインブランチに戻る
git switch 田中太郎

# Sprint 2 用のブランチを作成
git switch -c 田中太郎-sprint2
```

**ブランチ名のフォーマット:**
```
{あなたの名前}-sprint{N}

例：
- 田中太郎-sprint1
- 田中太郎-sprint2
- 田中太郎-sprint3
- 田中太郎-sprint4
```

### Step A: Sprint ディレクトリに移動

例: Sprint 1 の場合

```bash
cd r2b/training-sprint1
claude
```

### Step B: Learn フェーズ（Webapp でクイズ）

**学習は Webapp 上で行います。**

```bash
# Webapp が起動していない場合は起動
cd ../
./webapp/run.sh
# ブラウザで http://localhost:8000 を開く
```

**学習の流れ:**
1. Webapp の Sprint{N} タブ → クイズサブタブを開く
2. 未完了のサブカテゴリを選択
3. 3択クイズに回答
4. 正誤判定と解説を確認
5. 正解で次の項目へ、不正解は再挑戦
6. すべてのクイズに正解するまで繰り返し

**進捗確認:**
- ホームタブの「R2B Progress Viewer」で Phase 別進捗を確認
- Learn 列に合格数/問題数が表示される

### Step C: /r2b-design を実行（設計フェーズ）

Sprint ディレクトリに移動してから実行：

```bash
cd training-sprint1
/r2b-design
```

**実行内容:**

以下の10ステップの設計サブスキルを順番に実行：

```
1.  /design-business       → ビジネス要件
2.  /design-persona        → ペルソナ定義（複数）
3.  /design-journey        → ユーザージャーニーマップ
4.  /design-spec           → 仕様書（Page毎）
5.  /design-requirements   → 要件定義書 v1（Page毎）
6.  /design-ipo            → IPO一覧
7.  /design-data           → データ項目一覧
8.  /design-db             → DB設計書
9.  /design-requirements-v2 → 要件定義書 v2（Page毎）
10. /design-api            → API設計書
```

**出力（すべて `docs/requirements/` 配下）:**
- `business-requirements/business-requirements.md`
- `personas/{name}.md`（複数）
- `journey/journey.md`
- `specifications/{page}.md`（複数）
- `requirements-v1/{page}.md`（複数）
- `ipo/ipo.md`
- `data/data-list.md`
- `database/database-design.md`
- `requirements-v2/{page}.md`（複数）
- `api/api-design.md`

**Design フェーズ完了:**
全10ドキュメントが作成されたら、「Design完了」と伝えてください。
AIがPhase完了をDBに記録します。

### Step D: /r2b-build を実行（実装フェーズ）

```bash
/r2b-build
```

**実行内容:**
- `.claude/` ディレクトリに開発環境設定をセットアップ
- 設計ドキュメント（Step C で作成）を参照
- AI と協力してコードを実装
- `src/` ディレクトリ配下にソースコードを出力

**Build フェーズ完了:**
実装が完了したら、「Build完了」と伝えてください。
AIがPhase完了をDBに記録します。

### Step E: /r2b-review を実行（理解深化フェーズ）

```bash
/r2b-review
```

**実行内容:**
- 実装したコード（Step D）を解析
- フローチャートやシーケンス図を自動生成
- AI が概念を説明（EXPLAINED status）
- 理解度を質問で確認（UNDERSTOOD status）

**出力（`docs/review/` 配下）:**
- `step-1_overview.md`          # 全体像
- `step-2_overview-diagrams.md` # 俯瞰図解
- `step-3_deep-plan.md`         # 深掘り計画
- `step-4_deep-dive.md`         # 詳細図
- `step-5_deep-segment-plan.md` # セグメント計画（複雑な場合）
- `step-6_deep-dive-segment.md` # セグメント別詳細（複雑な場合）

**Review フェーズ完了:**
レビューが完了したら、「Review完了」と伝えてください。
AIがPhase完了をDBに記録します。

### Step F: 進捗を GitHub に push

各フェーズが完了したら、git で進捗を保存：

```bash
# 設計完了後
git add docs/requirements/
git commit -m "design: Sprint 1 設計ドキュメントを完成"
git push origin {あなたの名前}-sprint1

# 実装完了後
git add src/
git commit -m "feat: Sprint 1 の主要機能を実装"
git push origin {あなたの名前}-sprint1

# レビュー完了後
git add docs/review/
git commit -m "docs: コード解析と理解ドキュメントを作成"
git push origin {あなたの名前}-sprint1
```

### Step G: Sprint 完了時にメインブランチにマージ

**Sprint N の全フェーズが完了したら、メインブランチにマージ:**

```bash
# 1. メインブランチに戻る
git switch {あなたの名前}

# 2. Sprint N ブランチをメインブランチにマージ
git merge {あなたの名前}-sprint{N}

# 3. マージ結果を push
git push origin {あなたの名前}
```

**例: Sprint 1 完了時**

```bash
git switch 田中太郎
git merge 田中太郎-sprint1
git push origin 田中太郎
```

---

## 🔄 Sprint 2, 3, 4 に進む

### Sprint 完了 → 次の Sprint に進む流れ

各 Sprint 完了時に以下を繰り返します：

```
Sprint N 完了
    ↓
メインブランチに戻る
    ↓
Sprint N をメインブランチにマージ
    ↓
メインブランチから Sprint N+1 ブランチを作成
    ↓
Sprint N+1 を実行
```

### Sprint 2: メインブランチにマージしてから作成

```bash
# 1. メインブランチに戻る
git switch {あなたの名前}

# 2. Sprint 1 をメインブランチにマージ
git merge {あなたの名前}-sprint1

# 3. マージ結果を push
git push origin {あなたの名前}

# 4. Sprint 2 ブランチを作成
git switch -c {あなたの名前}-sprint2

# 5. Sprint 2 ディレクトリに移動
cd training-sprint2
```

### Sprint 2 での学習フロー

```bash
# 1. Webapp でクイズ（Learn フェーズ）
# http://localhost:8000 → Sprint 2 → クイズ

# 2. 設計
/r2b-design

# 3. 実装
/r2b-build

# 4. レビュー
/r2b-review

# 進捗を保存
git add docs/ src/
git commit -m "sprint2: 設計・実装・レビューを完了"
git push origin {あなたの名前}-sprint2
```

### Sprint 3 に進む

```bash
# 1. メインブランチに戻る
git switch {あなたの名前}

# 2. Sprint 2 をマージ
git merge {あなたの名前}-sprint2

# 3. push
git push origin {あなたの名前}

# 4. Sprint 3 ブランチを作成
git switch -c {あなたの名前}-sprint3

# 5. ディレクトリ移動と学習フロー
cd training-sprint3
# Webapp でクイズ → /r2b-design → /r2b-build → /r2b-review
git push origin {あなたの名前}-sprint3
```

### Sprint 4 に進む

```bash
# 1. メインブランチに戻る
git switch {あなたの名前}

# 2. Sprint 3 をマージ
git merge {あなたの名前}-sprint3

# 3. push
git push origin {あなたの名前}

# 4. Sprint 4 ブランチを作成
git switch -c {あなたの名前}-sprint4

# 5. ディレクトリ移動と学習フロー
cd training-sprint4
# Webapp でクイズ → /r2b-design → /r2b-build → /r2b-review
git push origin {あなたの名前}-sprint4
```

### すべて完了

```bash
# メインブランチに戻ってスプリント 4 をマージ
git switch {あなたの名前}
git merge {あなたの名前}-sprint4
git push origin {あなたの名前}

# 研修完了！🎉
```

---

## 📊 進捗確認

### Webapp で進捗を確認

```bash
cd r2b
./webapp/run.sh
# ブラウザで http://localhost:8000 を開く
```

**ホームタブ: R2B Progress Viewer**

Sprint × Phase のマトリックスで進捗を表示：

| Phase | 分母 | 計算方法 |
|-------|------|----------|
| Learn | クイズ問題数 | 合格したサブカテゴリ数 |
| Design | 10 | 作成済みドキュメント種類数 |
| Build | 1 | Phase完了で1 |
| Review | 1 | Phase完了で1 |
| Presentation | 1 | Phase完了で1 |
| **Status** | - | Sprint全体の状態（未開始/進行中/完了） |

---

## 🌿 ブランチ管理体系

### ブランチの階層構造

```
main（remote）
  ↓
{あなたの名前}  ← メインブランチ
  ├─ {あなたの名前}-sprint1
  │    ↓ (マージ) ↓
  ├─ {あなたの名前}-sprint2
  │    ↓ (マージ) ↓
  ├─ {あなたの名前}-sprint3
  │    ↓ (マージ) ↓
  └─ {あなたの名前}-sprint4
       ↓ (マージ) ↓
   {あなたの名前}（最終版、全Sprint統合）
```

各 Sprint 完了時に、Sprint ブランチをメインブランチにマージします。

### メインブランチ（初期化用）

```bash
git switch -c {あなたの名前}
# /r2b-init を実行
git push origin {あなたの名前}
```

**使用場面:**
- `/r2b-init` の実行（初期化は一度のみ）
- 他の Sprint ブランチを作成するときの基点

### Sprint 別ブランチ（作業用）

```bash
# メインブランチから分岐
git switch {あなたの名前}
git switch -c {あなたの名前}-sprint{N}

# Sprint での作業と push
git add docs/ src/
git commit -m "message"
git push origin {あなたの名前}-sprint{N}

# Sprint 完了時、メインブランチにマージ
git switch {あなたの名前}           # メインブランチに戻る
git merge {あなたの名前}-sprint{N}  # Sprint をマージ
git push origin {あなたの名前}      # メインブランチに push
```

**使用場面:**
- 各 Sprint での Learn（Webapp）→ /r2b-design → /r2b-build → /r2b-review 実行
- 設計ドキュメント・実装コードの commit
- **Sprint 完了時にメインブランチへのマージ**

**重要:** 各 Sprint 完了後は **必ずマージして、メインブランチに統合** してください。

---

## 🐛 トラブルシューティング

### Q: "/r2b-design コマンドが見つからない" または "Skill not found"

**原因:** プラグインがインストールされていないか、セッションが古い

**解決:**
```bash
# 1. 新しいセッションを開く
/exit
# 新しい Claude Code ウィンドウを開く

# 2. プラグインを再インストール
/install-plugin https://github.com/SALT2-Boost/r2b-marketplace
```

### Q: "/r2b-init を実行すると 'personal.sqlite not found' エラーが出る"

**原因:** training-sprint ディレクトリで実行している

**解決:**
```bash
# r2b ルートに戻る
cd r2b

# /r2b-init を実行（一度だけ）
/r2b-init
```

### Q: "db/personal.sqlite not found" エラーが Design フェーズで出る

**原因:** training-sprint{N} から実行しているが、DBパスが解決できていない

**解決:**
スクリプトは `training-sprint{N}/` から実行しても、親ディレクトリの `db/` を自動的に探索します。
エラーが続く場合は、r2b ルートで `/r2b-init` が完了しているか確認してください。

### Q: "docs/requirements ディレクトリが見つからない"

**原因:** training-sprint ディレクトリの構造が不完全

**解決:**
```bash
# ディレクトリを作成
mkdir -p docs/requirements
mkdir -p docs/review

# 設計スキルを再実行
/r2b-design
```

### Q: "git push が失敗する"

**原因:** リモートブランチがまだ存在しない、または認証が必要

**解決:**
```bash
# 初回は -u フラグで
git push -u origin {あなたの名前}

# 以降は通常の push
git push origin {あなたの名前}
```

### Q: "Webapp が起動しない"

**原因:** Python 依存関係がインストールされていない

**解決:**
```bash
cd r2b
pip install fastapi uvicorn
./webapp/run.sh
```

---

## 📝 推奨される学習ペース

### Sprint 1-4 の推奨スケジュール

| Phase | Duration | Tasks |
|-------|----------|-------|
| **Learn** | 2-3日 | Webapp でクイズを通じて全項目を習得 |
| **Design** | 2-3日 | /r2b-design で10の設計ドキュメントを作成 |
| **Build** | 4-5日 | /r2b-build でアプリケーションを実装 |
| **Review** | 2-3日 | /r2b-review で実装内容を理解・検証 |
| **Total** | 約2週間 | 1 Sprint を完了 |

**全体スケジュール（4 Sprint）:** 約8週間

---

## 💡 Tips

### 1. 現在のブランチを確認

```bash
git branch
# * {あなたの名前}-sprint1
#   {あなたの名前}
#   main
```

### 2. 毎日末に進捗を commit・push する

各 Sprint ブランチで作業中：

```bash
git add -A
git commit -m "進捗: {今日実施した内容}"
git push origin {あなたの名前}-sprint{N}
```

**例:**
```bash
# Sprint 1 での作業中
git push origin 田中太郎-sprint1

# Sprint 2 での作業中
git push origin 田中太郎-sprint2
```

### 3. Sprint 完了時のマージ（重要）

**各 Sprint の全フェーズが完了したら、必ずメインブランチにマージしてください：**

```bash
# 1. Sprint ブランチで全作業を push
git push origin {あなたの名前}-sprint{N}

# 2. メインブランチに戻る
git switch {あなたの名前}

# 3. Sprint をマージ
git merge {あなたの名前}-sprint{N}

# 4. メインブランチを push
git push origin {あなたの名前}

# 5. 次の Sprint がある場合、新しいブランチを作成
git switch -c {あなたの名前}-sprint{N+1}
cd ../training-sprint{N+1}
```

これにより、メインブランチに全 Sprint の成果が統合されます。

### 4. docs/ ディレクトリは重要

設計・レビューのドキュメントはすべて `docs/` に自動保存されます。これらは重要な成果物です。

### 5. git diff で進捗を確認

```bash
git diff main {あなたの名前}
# または
git diff main..{あなたの名前}
```

### 6. Webapp で進捗を可視化

学習後、webapp で全体進捗を確認：

⚠️ **重要**: `./webapp/run.sh` を実行する前に、**必ず r2b ルートで `/r2b-init` を実行してください**。`r2b-init` が完了していないと、エラーが発生します。

```bash
cd r2b
./webapp/run.sh
# ブラウザで http://localhost:8000 を開く
```

---

## ✅ セットアップ完了チェックリスト

**初期セットアップ（初回のみ）:**
- [ ] リポジトリを git clone した
- [ ] メインブランチ（{あなたの名前}）を作成した
- [ ] r2b ルートで Marketplace プラグインをインストールした
- [ ] /r2b-init を実行した
- [ ] db/personal.sqlite が作成されたか確認した
- [ ] Webapp が起動した（http://localhost:8000）

**Sprint 1 開始時:**
- [ ] メインブランチ（{あなたの名前}）から Sprint 1 ブランチ（{あなたの名前}-sprint1）を作成した
- [ ] training-sprint1 ディレクトリに移動した
- [ ] Webapp でクイズに回答した（Learn フェーズ）
- [ ] /r2b-design を実行した
- [ ] 設計ドキュメントが docs/requirements/ に保存されたか確認した（10種類）
- [ ] /r2b-build を実行した
- [ ] /r2b-review を実行した
- [ ] 進捗を git push した（git push origin {あなたの名前}-sprint1）

**Sprint 完了時:**
- [ ] メインブランチに戻った（git switch {あなたの名前}）
- [ ] Sprint ブランチをマージした（git merge {あなたの名前}-sprint{N}）
- [ ] メインブランチを push した（git push origin {あなたの名前}）

**Sprint 2-4:**
- [ ] 同じ流れを Sprint 2-4 で繰り返した
- [ ] 各 Sprint ブランチで進捗を push した
- [ ] 各 Sprint 完了時にメインブランチにマージした

すべてチェックできたら、研修を完了できます！ 🎉

---

**何か不明な点があれば、GitHub Issues で質問してください。**

https://github.com/SALT2-Boost/r2b/issues