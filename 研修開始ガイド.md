# R2B研修 — 完全セットアップガイド

研修生がR2B研修を開始するための、ステップバイステップの手引きです。

---

## 📋 全体の流れ

```
1. リポジトリをクローン
   ↓
2. メインブランチを作成（{あなたの名前}）
   ↓
3. (r2b ルートで) Marketplace 追加 → Plugin インストール → /r2b-init
   ↓
4. Sprint ループ（以下を Sprint 1→2→3→4 で繰り返）
   ├─ メインブランチから Sprint{N} ブランチを作成
   ├─ training-sprint{N} に cd
   ├─ Marketplace 削除・再追加
   ├─ /r2b-learn → /r2b-design → /r2b-build → /r2b-review
   ├─ git push で Sprint{N} ブランチに保存
   ├─ メインブランチに戻る（git switch {名前}）
   ├─ Sprint{N} をメインブランチにマージ（git merge）
   ├─ メインブランチを push
   └─ 次の Sprint{N+1} を作成（git switch -c）
   ↓
5. Sprint 1, 2, 3, 4 をすべて完了
   ↓
6. メインブランチに最終 push（全 Sprint の成果が統合）
```

---

## 🚀 ステップ1: リポジトリをクローン

### 1-1. GitHub から r2b リポジトリをクローン

```bash
git clone git@github.com:SALT2-Boost/r2b.git
cd r2b
```

**クローンされたディレクトリ構造：**

```
r2b/
├── schema-master.sql       # マスターDBスキーマ
├── schema-personal.sql     # 個人DBスキーマ
├── master.sql              # マスターDB初期データ
├── master.sqlite           # マスターDB（初期化済み）
├── webapp/                 # R2B Progress Viewer
├── sprint1/                # Sprint 1 リポジトリ
│   ├── src/                # ソースコード
│   ├── docs/               # 設計・学習ドキュメント
│   │   ├── requirements/   # 設計書
│   │   └── learn/          # 学習記録
│   └── README.md
├── sprint2/                # Sprint 2 リポジトリ
├── sprint3/                # Sprint 3 リポジトリ
└── sprint4/                # Sprint 4 リポジトリ
```

---

## 👤 ステップ2: メインブランチを作成（初期化用）

### 2-1. メインブランチを作成

```bash
git switch -c {あなたの名前}
```

例：
```bash
git switch -c 田中太郎
```

### 2-2. ブランチが作成されたか確認

```bash
git branch
# 出力例：
#   main
# * 田中太郎
```

このブランチで `/r2b-init` を実行します。

---

## 🌳 ステップ2.5: Sprint 用ブランチについて

**ブランチ命名ルール:**

各 Sprint に進む前に、Sprint 専用のサブブランチを作成します：

```
main
  ↓
{あなたの名前}  (初期化用・/r2b-init はここで実行)
  ↓
{あなたの名前-sprint1}  ← Sprint 1 作業用
{あなたの名前-sprint2}  ← Sprint 2 作業用
{あなたの名前-sprint3}  ← Sprint 3 作業用
{あなたの名前-sprint4}  ← Sprint 4 作業用
```

**詳細は後述の「各 Sprint での学習フロー」を参照**

---

## 🔌 ステップ3: R2B プラグインをセットアップ（r2b ルートで一度だけ実行）

### 3-1. Claude Code で r2b ディレクトリを開く

```bash
# r2b リポジトリのルートディレクトリで Claude Code を起動
cd r2b
claude code .
```

### 3-2. Marketplace にリポジトリを追加

Claude Code のターミナルで以下を実行：

```bash
/plugin marketplace add https://github.com/SALT2-Boost/r2b-docs.git#build
```

**確認:**
```bash
/plugin marketplace list
# r2b が表示される
```

### 3-3. プラグインをインストール

```bash
/plugin install r2b
```

インストール方法を選択する画面が表示されたら、**「Only for you, only in this repo」** を選択してください。

### 3-4. 🔄 セッションを再起動（重要）

Claude Code 内でセッションを終了し、新しいセッションを開く：

```bash
exit
# ターミナルが閉じたら、新しい Claude Code セッションを開く
```

### 3-5. プラグインが有効になったか確認

新しいセッションで：

```bash
/plugin list
# r2b が表示されるか確認
```

### 3-6. /r2b-init を実行（初期化は一度だけ）

```bash
/r2b-init
```

**実行内容:**
- 研修者の名前を聞かれるので入力
- personal.sqlite が r2b/ ルートに作成される
- 環境チェック（Node.js, Python, VSCode, Git の確認）
- Claude Code の基本操作説明
- Sprint 1 の準備

完了メッセージが表示されたら、セットアップは終了です！

---

## ✅ 各Sprint での学習フロー

以下の手順を **Sprint 1, 2, 3, 4 で繰り返します。**

### Step 0: Sprint 用ブランチを作成

メインブランチから Sprint 専用のブランチを作成します：

例: Sprint 1 の場合

```bash
# メインブランチ（{あなたの名前}）にいることを確認
git branch
# * 田中太郎

# Sprint 1 用のブランチを作成
git switch -c 田中太郎-sprint1
```

例: Sprint 2 の場合

```bash
# メインブランチに戻る
git switch 田中太郎

# Sprint 2 用のブランチを作成
git switch -c 田中太郎-sprint2
```

**ブランチ名のフォーマット:**
```
{あなたの名前}-sprint{N}

例：
- 田中太郎-sprint1
- 田中太郎-sprint2
- 田中太郎-sprint3
- 田中太郎-sprint4
```

### Step A: Sprint ディレクトリに移動

例: Sprint 1 の場合

```bash
cd r2b/sprint1
claude code .
```

### Step B: Marketplace を削除・再追加（Claude側バグ対応）

⚠️ **重要**: Claude がマーケットプレイスを正しく認識しないバグが生じる場合があるため、毎回 Sprint に入った時に実行してください。

```bash
# 1. Marketplace を削除
/plugin marketplace remove r2b

# 2. 削除が完了したか確認
/plugin marketplace list
# (r2b が表示されない)

# 3. Marketplace を再度追加
/plugin marketplace add https://github.com/SALT2-Boost/r2b-docs.git#build

# 4. プラグインが有効か確認
/plugin list
# r2b が表示される
```

### Step C: /r2b-learn を実行（学習フェーズ）

```bash
/r2b-learn
```

**学習の流れ:**
1. 未完了のチェックリスト項目を表示
2. AI がクイズを出題
3. 回答を入力
4. フィードバックを受取
5. 自動的に次の項目へ進む
6. すべての項目が完了するまで繰り返し

**出力:**
- `docs/learn/{id}.md` に学習記録が自動保存

### Step D: /r2b-design を実行（設計フェーズ）

```bash
/r2b-design
```

**実行内容:**

以下の6つの設計サブスキルから選択：

```
1. /design-journey    → ユーザージャーニーマップ
2. /design-project    → プロジェクト概要（お題・技術スタック）
3. /design-functions  → 機能一覧
4. /design-ui         → UI仕様・画面遷移
5. /design-data       → データ仕様・ER図
6. /design-api        → API仕様
```

**推奨実行順序:**

```bash
1. /design-project    # まず全体像を定義
2. /design-journey    # ユーザー視点で理解
3. /design-functions  # 必要な機能を列挙
4. /design-ui         # 画面設計
5. /design-data       # データモデル設計
6. /design-api        # 外部インターフェース設計
```

**出力:**
- `docs/requirements/journey.md`
- `docs/requirements/project.md`
- `docs/requirements/functions.md`
- `docs/requirements/ui.md`
- `docs/requirements/data.md`
- `docs/requirements/api.md`

### Step E: /r2b-build を実行（実装フェーズ）

```bash
/r2b-build
```

**実行内容:**
- 設計ドキュメント（Step D で作成）を参照
- AI と協力してコードを実装
- `src/` ディレクトリ配下にソースコードを出力

### Step F: /r2b-review を実行（理解深化フェーズ）

```bash
/r2b-review
```

**実行内容:**
- 実装したコード（Step E）を解析
- フローチャートやシーケンス図を自動生成
- AI が概念を説明（EXPLAINED status）
- 理解度を質問で確認（UNDERSTOOD status）

**出力:**
- `docs/review/step-0_overview.md`      # 全体像
- `docs/review/step-1_diagrams.md`      # 図解
- `docs/review/step-2_deep-dive.md`     # 深掘り解析

### Step G: 進捗を GitHub に push

各フェーズが完了したら、git で進捗を保存：

```bash
# 学習完了後
git add docs/learn/
git commit -m "learn: Sprint 1 Domain 0 の学習を完了"
git push origin {あなたの名前}-sprint1

# 設計完了後
git add docs/requirements/
git commit -m "design: プロジェクト概要と設計ドキュメントを完成"
git push origin {あなたの名前}-sprint1

# 実装完了後
git add src/
git commit -m "feat: Sprint 1 の主要機能を実装"
git push origin {あなたの名前}-sprint1

# レビュー完了後
git add docs/review/
git commit -m "docs: コード解析と理解ドキュメントを作成"
git push origin {あなたの名前}-sprint1
```

### Step H: Sprint 完了時にメインブランチにマージ

**Sprint N の全フェーズが完了したら、メインブランチにマージ:**

```bash
# 1. メインブランチに戻る
git switch {あなたの名前}

# 2. Sprint N ブランチをメインブランチにマージ
git merge {あなたの名前}-sprint{N}

# 3. マージ結果を push
git push origin {あなたの名前}
```

**例: Sprint 1 完了時**

```bash
git switch 田中太郎
git merge 田中太郎-sprint1
git push origin 田中太郎
```

---

## 🔄 Sprint 2, 3, 4 に進む

### Sprint 完了 → 次の Sprint に進む流れ

各 Sprint 完了時に以下を繰り返します：

```
Sprint N 完了
    ↓
メインブランチに戻る
    ↓
Sprint N をメインブランチにマージ
    ↓
メインブランチから Sprint N+1 ブランチを作成
    ↓
Sprint N+1 を実行
```

### Sprint 2: メインブランチにマージしてから作成

```bash
# 1. メインブランチに戻る
git switch {あなたの名前}

# 2. Sprint 1 をメインブランチにマージ
git merge {あなたの名前}-sprint1

# 3. マージ結果を push
git push origin {あなたの名前}

# 4. Sprint 2 ブランチを作成
git switch -c {あなたの名前}-sprint2

# 5. Sprint 2 ディレクトリに移動
cd ../sprint2
```

### Sprint 2 での学習フロー

```bash
/plugin marketplace remove r2b
/plugin marketplace add https://github.com/SALT2-Boost/r2b-docs.git#build
/r2b-learn    # 学習
/r2b-design   # 設計
/r2b-build    # 実装
/r2b-review   # レビュー

# 進捗を保存
git add docs/ src/
git commit -m "sprint2: 学習・設計・実装・レビューを完了"
git push origin {あなたの名前}-sprint2
```

### Sprint 3 に進む

```bash
# 1. メインブランチに戻る
git switch {あなたの名前}

# 2. Sprint 2 をマージ
git merge {あなたの名前}-sprint2

# 3. push
git push origin {あなたの名前}

# 4. Sprint 3 ブランチを作成
git switch -c {あなたの名前}-sprint3

# 5. ディレクトリ移動と学習フロー
cd ../sprint3
/r2b-learn && /r2b-design && /r2b-build && /r2b-review
git push origin {あなたの名前}-sprint3
```

### Sprint 4 に進む

```bash
# 1. メインブランチに戻る
git switch {あなたの名前}

# 2. Sprint 3 をマージ
git merge {あなたの名前}-sprint3

# 3. push
git push origin {あなたの名前}

# 4. Sprint 4 ブランチを作成
git switch -c {あなたの名前}-sprint4

# 5. ディレクトリ移動と学習フロー
cd ../sprint4
/r2b-learn && /r2b-design && /r2b-build && /r2b-review
git push origin {あなたの名前}-sprint4
```

### すべて完了

```bash
# メインブランチに戻ってスプリント 4 をマージ
git switch {あなたの名前}
git merge {あなたの名前}-sprint4
git push origin {あなたの名前}

# 研修完了！🎉
```

---

## 📊 進捗確認コマンド

### /r2b-status で進捗を確認

各 Sprint で学習後、進捗を可視化：

```bash
/r2b-status
```

**出力例:**

```
📊 Sprint 1 進捗

全体: ████████░░░░░░░░░░  40%

Domain別:
- Domain 0: ██████████░░░░░░  60% (30/50)
- Domain 1: ████░░░░░░░░░░░░  20% (10/50)
- Domain 2: ░░░░░░░░░░░░░░░░   0% (0/50)

残り項目: 60/150
```

---

## 🌿 ブランチ管理体系

### ブランチの階層構造

```
main（remote）
  ↓
{あなたの名前}  ← メインブランチ
  ├─ {あなたの名前}-sprint1
  │    ↓ (マージ) ↓
  ├─ {あなたの名前}-sprint2
  │    ↓ (マージ) ↓
  ├─ {あなたの名前}-sprint3
  │    ↓ (マージ) ↓
  └─ {あなたの名前}-sprint4
       ↓ (マージ) ↓
   {あなたの名前}（最終版、全Sprint統合）
```

各 Sprint 完了時に、Sprint ブランチをメインブランチにマージします。

### メインブランチ（初期化用）

```bash
git switch -c {あなたの名前}
# /r2b-init を実行
git push origin {あなたの名前}
```

**使用場面:**
- `/r2b-init` の実行（初期化は一度のみ）
- 他の Sprint ブランチを作成するときの基点

### Sprint 別ブランチ（作業用）

```bash
# メインブランチから分岐
git switch {あなたの名前}
git switch -c {あなたの名前}-sprint{N}

# Sprint での作業と push
git add docs/ src/
git commit -m "message"
git push origin {あなたの名前}-sprint{N}

# Sprint 完了時、メインブランチにマージ
git switch {あなたの名前}           # メインブランチに戻る
git merge {あなたの名前}-sprint{N}  # Sprint をマージ
git push origin {あなたの名前}      # メインブランチに push
```

**使用場面:**
- 各 Sprint での /r2b-learn → /r2b-design → /r2b-build → /r2b-review 実行
- 設計ドキュメント・実装コードの commit
- **Sprint 完了時にメインブランチへのマージ**

**重要:** 各 Sprint 完了後は **必ずマージして、メインブランチに統合** してください。

### さらに詳細なトピックブランチ（オプション）

複雑な実装が必要な場合、Sprint ブランチから さらに細分化：

```bash
# Sprint 1 ブランチから UI 設計用のトピックブランチを作成
git switch {あなたの名前}-sprint1
git switch -c {あなたの名前}-sprint1-ui-design

# 作業後、Sprint 1 ブランチにマージ
git switch {あなたの名前}-sprint1
git merge {あなたの名前}-sprint1-ui-design

# Sprint 1 ブランチを push
git push origin {あなたの名前}-sprint1
```

---

## 🐛 トラブルシューティング

### Q: "/r2b-learn コマンドが見つからない" または "Skill not found"

**原因:** プラグインがインストールされていないか、セッションが古い

**解決:**
```bash
# 1. プラグインが有効か確認
/plugin list

# 2. インストールされていない場合はインストール
/plugin install r2b

# 3. 新しいセッションを開く
exit
# 新しい Claude Code ウィンドウを開く
```

### Q: "Marketplace にリポジトリが見つからない"

**原因:** Marketplace が追加されていない、または削除されている

**解決:**
```bash
# Marketplace を追加
/plugin marketplace add https://github.com/SALT2-Boost/r2b-docs.git#build

# 確認
/plugin marketplace list
```

### Q: "/r2b-init を実行すると 'personal.sqlite not found' エラーが出る"

**原因:** まず r2b ルートディレクトリで /r2b-init を実行していないか、sprint ディレクトリで実行している

**解決:**
```bash
# r2b ルートに戻る
cd r2b

# /r2b-init を実行（一度だけ）
/r2b-init
```

### Q: "docs/requirements ディレクトリが見つからない"

**原因:** sprint ディレクトリの構造が不完全

**解決:**
```bash
# ディレクトリを作成
mkdir -p docs/requirements
mkdir -p docs/learn

# 設計スキルを再実行
/r2b-design
```

### Q: "マーケットプレイスのバグで /r2b-learn が実行されない"

**Claude側の既知バグ:** Marketplace が正しくリロードされない場合がある

**解決:**
```bash
# Marketplace を削除
/plugin marketplace remove r2b

# 再度追加
/plugin marketplace add https://github.com/SALT2-Boost/r2b-docs.git#build

# スキルが見えるか確認
/plugin list

# 再度実行
/r2b-learn
```

### Q: "git push が失敗する"

**原因:** リモートブランチがまだ存在しない、または認証が必要

**解決:**
```bash
# 初回は -u フラグで
git push -u origin {あなたの名前}

# 以降は通常の push
git push origin {あなたの名前}
```

---

## 📝 推奨される学習ペース

### Sprint 1-4 の推奨スケジュール

| Phase | Duration | Tasks |
|-------|----------|-------|
| **Learn** | 2-3日 | /r2b-learn で全チェックリスト項目を習得 |
| **Design** | 2-3日 | /r2b-design で6つの設計ドキュメントを作成 |
| **Build** | 4-5日 | /r2b-build でアプリケーションを実装 |
| **Review** | 2-3日 | /r2b-review で実装内容を理解・検証 |
| **Total** | 約2週間 | 1 Sprint を完了 |

**全体スケジュール（4 Sprint）:** 約8週間

---

## 🔗 関連ドキュメント

- [R2B プラグイン使用ガイド](./R2B%20—%20プラグイン使用ガイド.md) — コマンド詳細説明
- [システム要件](../spec/system-requirements.md)
- [用語定義](../spec/ubiquitous-language.md)

---

## 💡 Tips

### 1. 現在のブランチを確認

```bash
git branch
# * {あなたの名前}-sprint1
#   {あなたの名前}
#   main
```

### 2. 毎日末に進捗を commit・push する

各 Sprint ブランチで作業中：

```bash
git add -A
git commit -m "進捗: {今日実施した内容}"
git push origin {あなたの名前}-sprint{N}
```

**例:**
```bash
# Sprint 1 での作業中
git push origin 田中太郎-sprint1

# Sprint 2 での作業中
git push origin 田中太郎-sprint2
```

### 3. Sprint 完了時のマージ（重要）

**各 Sprint の全フェーズが完了したら、必ずメインブランチにマージしてください：**

```bash
# 1. Sprint ブランチで全作業を push
git push origin {あなたの名前}-sprint{N}

# 2. メインブランチに戻る
git switch {あなたの名前}

# 3. Sprint をマージ
git merge {あなたの名前}-sprint{N}

# 4. メインブランチを push
git push origin {あなたの名前}

# 5. 次の Sprint がある場合、新しいブランチを作成
git switch -c {あなたの名前}-sprint{N+1}
cd ../sprint{N+1}
```

これにより、メインブランチに全 Sprint の成果が統合されます。

### 4. docs/ ディレクトリは重要

設計・学習・レビューのドキュメントはすべて `docs/` に自動保存されます。これらは重要な成果物です。

### 5. git diff で進捗を確認

```bash
git diff main {あなたの名前}
# または
git diff main..{あなたの名前}
```

### 6. Webapp で進捗を可視化

学習後、webapp で全体進捗を確認：

⚠️ **重要**: `./webapp/run.sh` を実行する前に、**必ず r2b ルートで `/r2b-init` を実行してください**。`r2b-init` が完了していないと、エラーが発生します。

```bash
cd r2b
./webapp/run.sh
# ブラウザで http://localhost:8000 を開く
```

---

## ✅ セットアップ完了チェックリスト

**初期セットアップ（初回のみ）:**
- [ ] リポジトリを git clone した
- [ ] メインブランチ（{あなたの名前}）を作成した
- [ ] r2b ルートで Marketplace を追加した
- [ ] プラグインをインストールした
- [ ] セッションを再起動した
- [ ] /r2b-init を実行した
- [ ] personal.sqlite が作成されたか確認した

**Sprint 1 開始時:**
- [ ] メインブランチ（{あなたの名前}）から Sprint 1 ブランチ（{あなたの名前}-sprint1）を作成した
- [ ] Sprint 1 ディレクトリに移動した
- [ ] Marketplace を削除・再追加した
- [ ] /r2b-learn を実行した
- [ ] 学習内容が docs/learn/ に保存されたか確認した
- [ ] /r2b-design を実行した
- [ ] 設計ドキュメントが docs/requirements/ に保存されたか確認した
- [ ] /r2b-build を実行した
- [ ] /r2b-review を実行した
- [ ] 進捗を git push した（git push origin {あなたの名前}-sprint1）

**Sprint 2-4:**
- [ ] 同じ流れを Sprint 2-4 で繰り返した
- [ ] 各 Sprint ブランチで進捗を push した

すべてチェックできたら、研修を完了できます！ 🎉

---

**何か不明な点があれば、GitHub Issues で質問してください。**

https://github.com/SALT2-Boost/r2b-docs/issues
